# Example environment variables
# Copy to .env and fill values for local development or production secrets

# Vercel-specific (if deploying via Vercel)
# VERCEL_TOKEN=

# App settings
# NODE_ENV=production
# PORT=3000

# API keys (do NOT commit real keys)
# OPENAI_API_KEY=
